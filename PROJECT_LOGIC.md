# 📋 ЛОГИКА ИНТЕРФЕЙСА AI AGENT PLATFORM

**Дата создания:** 2025-11-22
**Статус:** В разработке

---

## 🎯 ОБЩАЯ ИНФОРМАЦИЯ

### Технологии
- **Frontend:** React 19.2.0 + TypeScript + Tailwind CSS + Vite
- **Backend:** Express.js + better-sqlite3 + JWT Auth
- **CRM:** Kommo (amoCRM)

### Принципы
- ✅ Фронтенд УЖЕ ГОТОВ - только подключаем логику
- ✅ НЕ создаем новые страницы - работаем с существующими
- ✅ Данные подтягиваются из CRM пользователя
- ❌ Теги из CRM НЕ подтягиваем
- ❌ Существующие сделки/контакты НЕ отображаем в списках

---

## 📄 СТРАНИЦА: РЕДАКТИРОВАНИЕ АГЕНТА

**Путь:** `/agents/:id/edit`
**Компонент:** `AgentEditor.tsx`

### ВКЛАДКА: ОСНОВНОЕ (Main)

#### 🔷 БЛОК 1: Профиль агента

**Элементы:**
```
├─ Название* (текстовое поле)
│  └─ Обязательное поле
│
├─ Toggle "Активно"
│  ├─ ВКЛ: Агент работает и отвечает на сообщения
│  └─ ВЫКЛ: Агент не работает
│
└─ Инструкции для агента* (textarea)
   ├─ Описание: "Начальные инструкции по тону, стилю и ответам
   │             вашего агента. Вы также можете добавить общие
   │             сведения о компании, чтобы помочь агенту отвечать
   │             более точно."
   └─ Агент использует ЭТИ инструкции + дополнительные данные
      (воронки, каналы, база знаний)
```

**Сохранение:**
- Поля: `name`, `isActive`, `systemInstructions`
- Таблица: `agents`

---

#### 🔷 БЛОК 2: Взаимодействие

**Элементы:**
```
└─ Checkbox "Проверять перед отправкой"
   ├─ ВКЛ: Агент НЕ отправляет автоматически
   │      ├─ Генерирует текст ответа В CRM
   │      └─ Менеджер проверяет и отправляет вручную
   └─ ВЫКЛ: Агент отправляет автоматически
```

**Сохранение:**
- Поле: `checkBeforeSend` (добавить в агента)
- Таблица: `agents`

---

#### 🔷 БЛОК 3: Настройки воронок

**Кнопка "Синхронизировать настройки CRM":**
```
При нажатии:
├─ API: POST /api/crm/sync
│  └─ Параметры: { agentId, crmType: 'Kommo' }
│
├─ Подтягивает из CRM пользователя:
│  ├─ Все воронки (pipelines)
│  └─ Все этапы каждой воронки (stages)
│
└─ Сохраняет в:
   └─ agents.crmData (JSON): { pipelines: [...], channels: [...] }
```

**Список воронок:**
```
Каждая воронка (раскрывающаяся):
├─ Заголовок воронки (например: "GENERATION LEAD")
├─ Toggle "Активно"
│  ├─ ВКЛ: Агент работает в этой воронке
│  └─ ВЫКЛ: Агент НЕ работает в этой воронке
│
└─ При раскрытии → Список этапов (checkboxes)
   └─ Пользователь выбирает на каких этапах агент работает
```

**Откуда берутся воронки:**
- Источник: CRM пользователя (Kommo API)
- До синхронизации: MOCK_PIPELINES (моковые данные)
- После синхронизации: Реальные воронки из CRM

**Сохранение:**
- Поле: `pipelineSettings` (JSON)
  ```json
  {
    "pipeline_id_1": {
      "name": "GENERATION LEAD",
      "active": true,
      "stages": ["stage_1", "stage_2", "stage_3"]
    }
  }
  ```

---

#### 🔷 БЛОК 4: Каналы

**Кнопка "Синхронизировать настройки CRM":**
```
При нажатии:
├─ Та же функция что и в Блоке 3 (УНИВЕРСАЛЬНАЯ)
├─ Подтягивает из CRM:
│  └─ Все каналы которые использует пользователь
│     (WhatsApp, Telegram, Instagram, Facebook, Email, Viber, и т.д.)
│
└─ Сохраняет в:
   └─ agents.crmData (JSON): { channels: [...] }
```

**Элементы:**
```
├─ Toggle "Все каналы"
│  ├─ ВКЛ: Агент работает во ВСЕХ каналах
│  └─ ВЫКЛ: Показывается выпадающий список
│            └─ Пользователь выбирает конкретные каналы
│
└─ Выпадающий список каналов (MultiSelect)
   └─ Источник: CRM пользователя (из channelSettings)
```

**Откуда берутся каналы:**
- Источник: CRM пользователя (Kommo API)
- До синхронизации: MOCK_CHANNELS (моковые данные)
- После синхронизации: Реальные каналы из CRM

**Сохранение:**
- Поле: `channelSettings` (JSON)
  ```json
  {
    "allChannels": false,
    "selected": ["whatsapp", "telegram", "instagram"]
  }
  ```

---

#### 🔷 БЛОК 5: База знаний

**Элементы:**
```
├─ Toggle "Разрешить доступ ко всем категориям"
│  ├─ ВКЛ: Агент имеет доступ КО ВСЕМ категориям
│  └─ ВЫКЛ: Показывается выпадающий список
│            └─ Выбираем какие папки агент будет использовать
│
├─ Toggle "Создать задачу, если ответ не найден"
│  ├─ ВЫКЛ: Задача НЕ создается
│  └─ ВКЛ: Создается задача в CRM
│           ├─ На: Ответственного менеджера сделки
│           └─ Содержание: На что агент не нашел ответ
│
├─ Поле ввода текста (textarea)
│  ├─ Название: "Сообщение клиенту, если ответ не найден"
│  ├─ Подсказка: "Это сообщение будет показано, когда агент
│  │              не сможет найти релевантную информацию
│  │              в базе знаний."
│  └─ Текст отправляется клиенту когда агент не знает ответа
│
└─ Кнопка "Открыть базу знаний"
   └─ Переход на страницу "Статьи" (kb-articles)
```

**Сохранение:**
- Поле: `kbSettings` (JSON)
  ```json
  {
    "allCategories": true,
    "selectedCategories": ["cat_1", "cat_2"],
    "createTaskIfNotFound": true,
    "noAnswerMessage": "Текст сообщения..."
  }
  ```

---

#### 🔷 БЛОК 6: Футер

**Элементы:**
```
├─ Кнопка "Сохранить"
│  └─ Сохраняет ВСЕ изменения агента
│     └─ API: PUT /api/agents/:id
│
└─ Кнопка "Отмена"
   └─ Отменяет изменения и возвращает назад
```

---

### ВКЛАДКА: СДЕЛКИ И КОНТАКТЫ (Deals)

#### 🔷 СЕКЦИЯ 1: Настройки доступа к данным (ЧТЕНИЕ)

**Кнопка "Синхронизировать настройки CRM":**
```
При нажатии:
├─ Та же универсальная функция
├─ Подтягивает:
│  ├─ Поля сделок из CRM
│  └─ Поля контактов из CRM
│
└─ До синхронизации:
   ├─ DEAL_FIELDS (20 полей)
   └─ CONTACT_FIELDS (10 полей)
```

**Подсекция: Данные сделки**
```
├─ MultiSelect "Выберите поля сделки"
│  ├─ Источник: DEAL_FIELDS (или из CRM после синхронизации)
│  └─ Пользователь выбирает какие поля агент может ЧИТАТЬ
│
└─ Подсказка: "Выбирайте только необходимые поля.
              Дополнительные поля добавляют лишний контекст
              и могут снизить точность ответов"
```

**Подсекция: Данные контакта**
```
├─ MultiSelect "Выберите поля контакта"
│  ├─ Источник: CONTACT_FIELDS (или из CRM после синхронизации)
│  └─ Пользователь выбирает какие поля агент может ЧИТАТЬ
│
└─ Подсказка: "Выбирайте только необходимые поля..."
```

**Сохранение:**
- Поле: `crmData` (JSON)
  ```json
  {
    "dealReadFields": ["deal_stage", "deal_budget", "deal_name"],
    "contactReadFields": ["contact_name", "contact_phone", "contact_email"]
  }
  ```

---

#### 🔷 СЕКЦИЯ 2: Настройки ввода данных (ЗАПИСЬ)

**Подсекция: Данные сделки**
```
├─ Описание: "Задайте правила автоматического обновления
│             полей сделки во время разговора"
│
└─ Список правил (динамический):
   └─ Каждое правило:
      ├─ Select "Поле" → Выбор поля из DEAL_FIELDS
      ├─ Input "Условие обновления" → Текстовое условие
      │  └─ Пример: "когда клиент упоминает цену"
      ├─ Toggle "Перезаписать существующее значение"
      └─ Кнопки: ↑ ↓ 🗑️ (вверх, вниз, удалить)
```

**Подсекция: Данные контакта**
```
├─ Описание: "Определите правила автоматического обновления
│             полей контакта во время разговора"
│
└─ Список правил (динамический):
   └─ Каждое правило:
      ├─ Select "Поле" → Выбор поля из CONTACT_FIELDS
      ├─ Input "Условие обновления" → Текстовое условие
      │  └─ Пример: "когда клиент называет своё имя"
      ├─ Toggle "Перезаписать существующее значение"
      └─ Кнопки: ↑ ↓ 🗑️
```

**Сохранение:**
- Поле: `crmData` (JSON)
  ```json
  {
    "dealUpdateRules": [
      {
        "fieldId": "deal_budget",
        "condition": "когда клиент упоминает цену",
        "overwrite": true
      }
    ],
    "contactUpdateRules": [
      {
        "fieldId": "contact_name",
        "condition": "когда клиент называет своё имя",
        "overwrite": true
      }
    ]
  }
  ```

---

### ВКЛАДКА: ТРИГГЕРЫ (Triggers)

**Статус:** Ожидает описания логики

---

### ВКЛАДКА: ЦЕПОЧКИ (Chains)

**Статус:** Ожидает описания логики

---

### ВКЛАДКА: ИНТЕГРАЦИИ (Integrations)

**Статус:** Ожидает описания логики

---

### ВКЛАДКА: ДОПОЛНИТЕЛЬНО (Advanced)

**Статус:** Ожидает описания логики

---

## 🔌 API ENDPOINTS

### CRM Sync
```
POST /api/crm/sync
Body: { agentId: string, crmType: string }

Действия:
1. Подключается к CRM пользователя (Kommo)
2. Получает:
   - Воронки (pipelines) + этапы (stages)
   - Каналы (channels)
   - Поля сделок (deal fields)
   - Поля контактов (contact fields)
3. Создает тестовые данные (3 контакта + 3 сделки)
4. Обновляет агента:
   - crmConnected = true
   - crmType = 'Kommo'
   - crmData = { pipelines, channels, fields, ... }
5. Возвращает:
   { message, contacts: 3, deals: 3, crmType }
```

---

## 📊 БАЗА ДАННЫХ

### Таблица: agents

**Поля CRM:**
```sql
crmType TEXT,              -- 'Kommo', 'Bitrix24', и т.д.
crmConnected INTEGER,      -- 0/1 (false/true)
crmData TEXT,              -- JSON с данными CRM
pipelineSettings TEXT,     -- JSON настройки воронок
channelSettings TEXT,      -- JSON настройки каналов
kbSettings TEXT            -- JSON настройки базы знаний
```

**Структура JSON полей:**

`crmData`:
```json
{
  "pipelines": [...],
  "channels": [...],
  "dealFields": [...],
  "contactFields": [...],
  "dealReadFields": [...],
  "contactReadFields": [...],
  "dealUpdateRules": [...],
  "contactUpdateRules": [...]
}
```

`pipelineSettings`:
```json
{
  "pipeline_id": {
    "name": "Название",
    "active": true,
    "stages": ["stage_1", "stage_2"]
  }
}
```

`channelSettings`:
```json
{
  "allChannels": false,
  "selected": ["whatsapp", "telegram"]
}
```

`kbSettings`:
```json
{
  "allCategories": true,
  "selectedCategories": ["cat_1"],
  "createTaskIfNotFound": true,
  "noAnswerMessage": "Текст..."
}
```

---

## 📝 ПРИМЕЧАНИЯ

- Все данные из CRM подтягиваются РЕАЛЬНЫЕ (не моки)
- После синхронизации моковые данные заменяются на реальные
- Кнопка "Синхронизировать настройки CRM" - УНИВЕРСАЛЬНАЯ (одна для всех блоков)
- Сохранение всех настроек происходит при нажатии "Сохранить" внизу страницы

---

**Конец документа**
